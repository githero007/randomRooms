{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",  // Change this to your actual entry point file
  "scripts": {
    "start": "node server.js",  // Add a start script to run your server
    "dev": "nodemon server.js",  // Optional: use nodemon for development
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1",
    "mongoose": "^8.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"  // Add nodemon to devDependencies if you're using it
  }
}
